Writes a file to the local file system and replaces the file when it exists.

- Read an existing file in this conversation before you overwrite it, or the write fails.
- Use Edit for changes to an existing file. Write is for new files and complete rewrites.
- Do not create documentation or README files unless the user asks for them.
